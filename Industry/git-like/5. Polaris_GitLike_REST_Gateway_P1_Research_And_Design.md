# Polaris Git-like REST Gateway P1 调研验证与实现设计

> 调研与设计日期：2026-05-20。本文基于 `3. Lakehouse_GitLike_Evolution_Roadmap_And_Design_Options.md` 与 `4. Polaris_Branch_Overlay_Demo_Research_And_Design.md`，分析 P0 branch overlay demo 是否能迁移到真实 LakeHouse 数据规模，并给出 P1 可分阶段实现的方案。P1 选择 Apache Polaris 作为主流 Catalog 基线，但不再放大 P0 manifest 模型，而是引入 branch-aware Iceberg REST Gateway 与外置权威 Version Store。

## 1. 方向选择与结论

本轮选择的方向是：**基于 Apache Polaris 1.4.1 做 branch-aware Iceberg REST Gateway，使用 PostgreSQL Version Store 保存 catalog-wide refs、commits、content values 与 indexes，把 P0 的流程验证升级为可迁移到实际数据规模的 P1 架构**。

结论先行：

- P0 demo 的整体工作流有研究价值，但不能直接迁移到实际 LakeHouse 数据规模。
- P0 的核心限制不是 Python 或 PowerShell 本身，而是架构语义：它把 catalog-wide branch 降级成“多张表同名 Iceberg table branch 的外部清单”。
- P1 不能只是把 `branches.json` 换成数据库；P1 必须引入独立权威 Version Store、ref HEAD、catalog commit、索引、幂等、锁、审计和可恢复物化链路。
- P1 继续选择 Polaris，因为它是 Iceberg REST-first 的主流 Catalog，能复用权限、存储配置、credential vending、管理 API 和 P0 已验证的运行基线。
- P1 不修改 Polaris core。所有 branch-aware 读写先收敛到 Gateway，Polaris 只作为 main 的兼容物化视图、权限/凭证基线和后续 P2 内置化参照。

P1 的最低功能目标是：

- 支持 `branch`：从 `main` 或指定 ref 创建 branch，创建成本与 catalog 表数量无关。
- 支持 `commit`：通过 Gateway 对某个 branch 提交 Iceberg 表更新，生成 catalog commit，main 不受 branch 写入污染。
- 支持 `publish-plan` 与 fast-forward `publish`：main 未漂移时用一次 ref HEAD CAS 发布，main 已漂移时拒绝。
- 支持基本可靠性：幂等键、CAS 冲突处理、branch lock / merge queue、审计、outbox、失败恢复。
- 支持基本性能：list/load/diff 走索引，不逐表采集 metadata table，不回放完整历史。

P1 明确不做完整 semantic merge，不承诺 Git 双父 merge graph，不做 P2 级 Polaris 内置版本层，不把 branch 维度硬塞进 Polaris 现有 table row。

## 2. P0 demo 现实性分析

P0（`Demo/branch_on_polaris`）验证了 Iceberg 表级 branch 编排工作流，但存在以下结构性限制，不能直接放大到真实数据规模：

| 限制 | 问题 | P1 解法 |
|---|---|---|
| 状态随 branch×table 膨胀 | manifest 体积与 branch 创建成本随表数线性增长 | O(1) ref pointer，不复制表集合 |
| 无服务端写入口 | 脚本串行执行，无并发控制、幂等、认证 | Gateway 作为唯一写入口 |
| 逐表 publish 非原子 | 多表发布中途失败导致 main 部分更新 | ref HEAD 一次 CAS |
| Polaris 不感知 branch | list/load/commit/credentials 等路径绕过 branch 语义 | Gateway 覆盖全部引擎读写路径 |
| 工程栈不适合生产 | Python + PowerShell + in-memory Polaris | JVM 服务 + PostgreSQL + Testcontainers |

P0 的可迁移内容是语义样本与验收用例，不是实现架构。

## 3. 产品与路线选择

P1 继续选择 Apache Polaris，方案选择 `3.md` 第二章中的 **REST/API 代理转换层 + 外置版本层**。更准确地说，P1 是一个 branch-aware Iceberg REST Gateway，内部持有外置 Version Store，外部对引擎表现为 Iceberg REST Catalog。

### 3.1 Catalog 选择

| 产品 / 路线 | 是否作为 P1 基线 | 判断 |
|---|---:|---|
| Apache Polaris | 是 | 与 P0 连续，代表 Iceberg REST-first 主流 Catalog。Polaris 1.4.1 是当前稳定基线，适合复用权限、存储配置、credential vending 和管理 API，同时作为 P2 内置版本层参照。 |
| Project Nessie | 否，作为语义标尺 | Nessie 已经原生 Git-like，适合借鉴 ref、commit、content value、merge、GC 和 Iceberg REST 接入方式，但本课题目标是增强主流非 Git-like Catalog。 |
| Apache Gravitino | 否 | 适合 catalog-of-catalogs、治理和 federation，但当前 Iceberg REST service 文档仍列出 multi table transaction 未实现，不适合作为 P1 branch/commit 基线。 |
| Unity Catalog OSS | 暂不选 | Delta Catalog-Managed Tables 很有价值，但 Delta CMT 和 UC managed table 的协议、客户端和存储入口侵入更深，不适合作为 P0 Iceberg/Polaris 的直接 P1。 |
| Lakekeeper | 可替代参照 | Rust Iceberg REST Catalog 与 credential vending 很强，但 P0 已选择 Polaris，P1 需要优先保持验证连续性。 |

选择 Polaris 的原因不是它天然支持 Git-like，而是它已经具备主流 Iceberg REST Catalog 的关键基础：

- Catalog / namespace / table 管理。
- 表名到 metadata pointer 的当前态管理。
- 原子更新 table metadata pointer 的基础。
- REST-compatible 多引擎入口。
- RBAC、storage configuration、credential vending 与管理 API。
- Docker image、Helm、PostgreSQL 等生产部署路径。

P1 在 Polaris 外部补 branch-aware Gateway，能先验证产品语义和性能模型；如果成立，P2 再把 Version Store 下沉到 Polaris core。

### 3.2 路线选择

`3.md` 第二章列出四类实现架构：Catalog 内置版本层、Catalog 外置版本层、REST/API 代理转换层、先做多表事务再扩展版本能力。P1 选择其中两者的组合：

```text
Spark / Trino / Flink / PyIceberg
  -> P1 branch-aware Iceberg REST Gateway
       -> PostgreSQL Version Store
       -> Polaris REST / Management API
       -> Object Storage
```

选择这个组合的原因：

- 只做外置版本层但不代理写入口，会被直接写 Polaris 绕过，不能保证 branch 隔离。
- 只做 REST 代理但没有完整 per-ref content value，会丢失历史分支的权威状态。
- 直接改 Polaris core 是 P2 范围，P1 成本过高。
- 先做多表事务再补 branch 会遇到历史模型不足；P1 已经要求至少具备 branch 和 commit，所以需要从一开始建立 refs/commits/content values。

P1 的架构原则是：**Gateway 是 Git-like 读写入口，Version Store 是 branch/commit 权威状态，Polaris 是 main 兼容物化视图与治理基线**。

## 4. P1 总体方案

### 4.1 架构图

```text
                     +--------------------+
Spark / Trino        |                    |
Flink / PyIceberg -> | P1 REST Gateway    |
Iceberg Java API     | branch-aware IRC   |
                     +---------+----------+
                               |
       +-----------------------+-----------------------+
       |                       |                       |
+------+-------+        +------+-------+        +------+-------+
| Version Store|        | Apache       |        | Object       |
| PostgreSQL   |        | Polaris      |        | Storage      |
| refs/commits |        | main view    |        | metadata/data|
| content/index|        | auth/cred    |        | S3/ADLS/GCS  |
+--------------+        +--------------+        +--------------+
```

P1 Gateway 对外暴露两组 API：

- Iceberg REST Catalog API 子集，供 Spark、Trino、Flink、PyIceberg 等引擎读写。
- P1 管理 API，供平台、CLI、UI 和 Agent 创建 branch、查看 diff、生成 publish-plan、执行 publish。

P1 Gateway 内部由六个模块组成：

| 模块 | 职责 |
|---|---|
| Ref Resolver | 从 header、path 或默认配置解析 `ref`，校验 branch/tag/hash 是否存在，生成 request context。 |
| Iceberg REST Adapter | 实现 P1 必需的 Iceberg REST 子集，处理 table load、commit、transaction commit、namespace/list、drop/rename 和 credentials pass-through。 |
| Version Store | PostgreSQL 持久化 refs、commits、content values、commit ops、indexes、幂等和审计。 |
| Commit Coordinator | 校验 requirements，写 Iceberg metadata，生成 catalog commit，通过 ref HEAD CAS 完成提交。 |
| Publish Coordinator | 生成 fast-forward plan，执行 branch lock / merge queue，CAS 推进 main，并写 outbox。 |
| Polaris Materializer | 将 main 的新状态物化到 Polaris，支持 outbox 重试、对账和诊断。 |

### 4.2 Ref 选择方式

P1 默认使用 HTTP header 选择 ref：

```http
X-Catalog-Ref: dev_a
```

选择 header 的原因是它不污染 namespace/table 名称，也不强行改变 Iceberg REST `prefix` 的含义。对无法稳定注入 header 的客户端，保留 path fallback：

```text
/iceberg/dev_a/v1/{prefix}/namespaces/sales/tables/orders
```

约定如下：

- 未提供 ref 时默认使用 `main`。
- `main` 可读写，但生产部署建议用 branch protection 禁止普通用户直写 main。
- tag 和 commit hash 只读。
- 所有 cache key、ETag、credential scope、audit event 必须包含 ref 或 resolved commit id。
- 禁止 namespace 编码 branch，例如 `dev_a.sales.orders`，避免污染业务 namespace。

### 4.3 Gateway 覆盖的 Iceberg REST 子集

P1 不需要一次覆盖全部 Iceberg REST OpenAPI，但必须覆盖能形成 branch/commit 闭环的路径。

| Iceberg REST 路径 | P1 行为 |
|---|---|
| `GET /v1/config` | 返回 Gateway 支持的 endpoint、warehouse 与必要 overrides。 |
| `GET /v1/{prefix}/namespaces` | 从 Version Store ref index 解析 namespace，可叠加 Polaris main 物化视图做 bootstrap。 |
| `GET /v1/{prefix}/namespaces/{namespace}` | 读取 namespace 元信息；P1 可先支持最小属性集。 |
| `GET /v1/{prefix}/namespaces/{namespace}/tables` | 从 ref index 按 namespace 列表返回表，不逐表加载 metadata。 |
| `GET /v1/{prefix}/namespaces/{namespace}/tables/{table}` | 从 ref HEAD index 解析 content value，返回该 ref 上的 Iceberg metadata 与 config。 |
| `POST /v1/{prefix}/namespaces/{namespace}/tables` | 支持创建表或 stage-create，生成 branch 上的新 content value。 |
| `POST /v1/{prefix}/namespaces/{namespace}/tables/{table}` | 支持单表 commit，校验 requirements，写 metadata，生成 catalog commit。 |
| `DELETE /v1/{prefix}/namespaces/{namespace}/tables/{table}` | 在目标 ref 上写 tombstone commit，不立即删除物理文件。 |
| `POST /v1/{prefix}/tables/rename` | 在目标 ref 上写 key rename commit，保留 content id。 |
| `POST /v1/{prefix}/transactions/commit` | 支持多表一次 catalog commit。 |
| `GET /v1/{prefix}/namespaces/{namespace}/tables/{table}/credentials` | 透传或委托 Polaris credential vending，但 scope 必须绑定 resolved ref/content。 |

如果某条路径暂不支持，Gateway 必须返回明确的 `UnsupportedOperationException`，不能静默转发到 Polaris，因为静默转发会绕过 ref 语义。

### 4.4 Polaris 的定位

P1 中 Polaris 不再是 branch 的事实源。它承担三类职责：

1. **治理基线**：继续管理 catalog、principal、role、storage config、credential vending 和现有管理 API。
2. **main 兼容物化视图**：给未接入 Gateway 的只读旧客户端提供当前 main 状态。
3. **P2 迁移参照**：P1 Version Store 的 refs/commits/content values 模型未来可映射到 Polaris 内置 persistence。

为了保证 P1 语义成立，生产部署必须限制直写 Polaris：

- 引擎写入只能访问 Gateway 的 Iceberg REST endpoint。
- Polaris REST endpoint 只允许 Gateway 和受控运维账号访问。
- 如果允许客户端直接写 Polaris，则 P1 只能承诺 Gateway 内部一致性，不能承诺全平台 branch 隔离。

## 5. 数据模型详细设计

P1 使用 PostgreSQL 16 作为 Version Store。数据库结构需要支持 O(1) branch 创建、CAS commit、按 ref 快速 list/load、diff、审计、幂等和 outbox。

### 5.1 核心表

#### `catalog_refs`

```text
catalog_refs(
  catalog_id,
  ref_name,
  ref_type,              -- BRANCH / TAG
  head_commit_id,
  policy_id,
  deleted,
  created_by,
  created_at,
  updated_at,
  version
)
```

关键约束：

- `(catalog_id, ref_name)` 唯一。
- `main` 是系统 branch，初始化时创建。
- 更新 `head_commit_id` 必须带 expected head，例如 `WHERE head_commit_id = :expected_head`。
- tag 默认不可变；如果需要移动 tag，必须有显式管理权限。

#### `catalog_commits`

```text
catalog_commits(
  commit_id,
  catalog_id,
  parent_commit_id,
  author,
  message,
  created_at,
  operation_summary,
  index_id,
  source_ref,
  request_id
)
```

P1 commit 采用单父模型。fast-forward publish 可以把 source branch head 直接赋给 main；如果后续要记录 publish 审计，可在 `operation_summary` 中记录 source ref 与 publish plan。P1 不实现 Git 双父 merge commit。

#### `catalog_content_values`

```text
catalog_content_values(
  value_id,
  catalog_id,
  content_id,
  content_key,           -- namespace.table
  content_type,          -- ICEBERG_TABLE / VIEW / NAMESPACE / TOMBSTONE
  metadata_location,
  snapshot_id,
  schema_id,
  spec_id,
  sort_order_id,
  table_uuid,
  table_location,
  format_version,
  last_sequence_number,
  metadata_fingerprint,
  metadata_summary_json,
  created_at
)
```

`content_id` 用于维持逻辑对象身份，尤其是 rename 场景。`content_key` 是某个 ref 上的可见路径。`metadata_location` 是 Iceberg 表在该 ref 上的权威 metadata JSON。

`metadata_summary_json` 存储核心字段摘要（schema fingerprint、partition spec 摘要、sort order id、properties 白名单），**不存储完整 Iceberg metadata JSON**；完整 metadata 始终以 `metadata_location` 指向的 object storage 文件为准。这个字段只用于 diff/list 的快速比较，不用于 `loadTable` 的权威返回。

#### `catalog_commit_ops`

```text
catalog_commit_ops(
  commit_id,
  content_key,
  op_type,               -- PUT / DELETE / RENAME / UNCHANGED
  expected_value_id,
  new_value_id,
  old_content_key,
  new_content_key
)
```

用途：

- 支持 diff。
- 支持 publish-plan。
- 支持 read-set validation。
- 支持审计和后续 revert。

P1 至少支持 `PUT`、`DELETE`、`RENAME`。`UNCHANGED` 可作为多表 transaction 的 read-set validation 扩展。

### 5.2 索引表

只保存 refs 与 commits 不足以支撑大规模读取。P1 需要显式索引。

```text
catalog_ref_indexes(
  index_id,
  catalog_id,
  commit_id,
  parent_index_id,
  index_kind,            -- FULL / INCREMENTAL
  object_count,
  stripe_count,
  created_at
)
```

```text
catalog_content_index(
  index_id,
  namespace,
  content_key,
  content_id,
  value_id,
  content_type,
  deleted
)
```

实现原则：

- 小 commit 生成 incremental index。
- compaction 阈值是 P1 默认配置：同一 ref 累积 **20 个 incremental index** 或 index 中 ops 总数超过 **1000** 时，触发 full compaction。Phase 2 只需实现阈值判断与测试；实际 full compaction 可在 Phase 3/5 随 index 查询能力落地，P2 可改为异步后台任务。
- `listTables(namespace)` 必须通过 `catalog_content_index` 的 `(index_id, namespace)` 查找。
- `loadTable(key)` 必须通过 `(index_id, content_key)` 查找。
- `diff(base, head)` 优先使用 commit ops；历史跨度超过 50 个 commits 时切换为 index 对比。

### 5.3 可靠性表

```text
idempotency_keys(
  key,
  request_hash,
  status,
  response_payload,
  created_at,
  expires_at
)
```

```text
branch_locks(
  catalog_id,
  ref_name,
  lock_type,
  owner,
  expires_at,
  fencing_token
)
```

```text
outbox_materializations(
  event_id,
  catalog_id,
  target_ref,
  commit_id,
  status,
  attempts,
  next_attempt_at,
  last_error,
  created_at,
  updated_at
)
```

```text
audit_events(
  event_id,
  catalog_id,
  ref_name,
  actor,
  action,
  commit_id,
  request_id,
  payload,
  created_at
)
```

这些表不是附加功能，而是 P1 能否从 demo 进入真实环境的分水岭。没有幂等、锁、审计和 outbox，P1 在失败重试、并发 publish 和物化失败时无法给出确定语义。

## 6. 核心流程设计

### 6.1 Bootstrap main

P1 初始化时需要从 Polaris 当前 main 状态生成 Version Store 的初始 commit。

```text
1. Gateway 用 Polaris REST / Iceberg REST 列出 namespace 与 table。
2. 对每张表 loadTable，读取 metadata_location、snapshot_id、schema_id、table_uuid 等。
3. 写入 catalog_content_values。
4. 生成 initial commit c0 与 full index i0。
5. 创建 catalog_refs(main -> c0)。
6. 写 audit event: BOOTSTRAP_MAIN。
```

这个过程可以离线执行。生产首次接入时，应在 bootstrap 窗口禁止对 Polaris 直接写入，避免初始快照不一致。

### 6.2 Create branch

创建 branch 只复制 source ref 的 HEAD 指针：

```text
POST /api/v1/catalogs/{catalog}/branches
{
  "name": "dev_a",
  "sourceRef": "main",
  "message": "create dev_a"
}
```

流程：

```text
1. resolve sourceRef -> head_commit_id
2. insert catalog_refs(dev_a -> source_head)
3. write audit event
4. return branch metadata
```

这一步不扫描全 catalog，不复制表集合，不创建 Iceberg table branch。因此 branch 创建是 O(1)，可以迁移到真实 catalog 规模。

### 6.3 Load table on ref

引擎读取某张表：

```http
GET /iceberg/v1/prod/namespaces/sales/tables/orders
X-Catalog-Ref: dev_a
```

流程：

```text
1. Ref Resolver 得到 ref=dev_a。
2. 读取 catalog_refs.head_commit_id。
3. 读取 commit.index_id。
4. 从 catalog_content_index 查 sales.orders -> value_id。
5. 从 catalog_content_values 读取 metadata_location 与 cached metadata summary。
6. 加载 Iceberg metadata JSON，返回 LoadTableResponse。
7. 返回的 config/credentials/cache metadata 必须绑定 ref 与 commit id。
```

如果表在该 ref 上是 tombstone，则返回 `NoSuchTableException`，即使 Polaris main 中仍能看到该表。

### 6.4 Commit table on branch

引擎提交单表更新：

```http
POST /iceberg/v1/prod/namespaces/sales/tables/orders
X-Catalog-Ref: dev_a
Idempotency-Key: branch-commit-001
```

流程：

```text
1. 校验 ref 可写，tag/hash 只读。
2. 校验 idempotency key；重复请求直接返回已记录响应。
3. 读取 ref expected HEAD 与 table expected content value。
4. 用 Iceberg Java API 校验 REST requirements 与 updates。
5. 使用 FileIO 写新的 Iceberg metadata JSON 到 object storage。
6. 在 PostgreSQL 事务中：
   a. insert catalog_content_values(new value)
   b. insert catalog_commits(parent = expected HEAD)
   c. insert catalog_commit_ops
   d. build incremental index
   e. update catalog_refs set head_commit_id = new_commit where head_commit_id = expected_head
   f. insert audit event
   g. record idempotency response
7. 如果 CAS 更新行数为 0，返回 409 conflict；客户端或 Gateway 按配置重试。
```

这个流程把 ref HEAD CAS 作为最终提交边界。对象存储中的 metadata JSON 先写入，若 DB CAS 失败，该 metadata 变成未引用对象，后续由 orphan cleanup 或 GC dry-run 清理。

### 6.5 Multi-table transaction commit

P1 应支持 Iceberg REST `POST /v1/{prefix}/transactions/commit`，因为真实 LakeHouse 场景经常要求多张表在同一次发布中保持一致。

流程与单表 commit 类似，但一次处理多个 table commit：

```text
1. 解析 ref 与 expected HEAD。
2. 校验所有 table requirements。
3. 为每张表写新的 metadata JSON。
4. 在一个 DB 事务中插入多个 content values 与 commit ops。
5. 生成一个 catalog commit。
6. 对 ref HEAD 做一次 CAS。
```

P1 的多表原子性指 Version Store 中的 catalog commit 原子可见。Polaris main 的物化可以滞后，但 Gateway 读写始终以 Version Store 为准。

### 6.6 Diff

P1 管理 API：

```text
POST /api/v1/catalogs/{catalog}/branches/{branch}/diff
{
  "baseRef": "main"
}
```

输出至少包含：

- changed tables。
- added tables。
- deleted tables。
- renamed tables。
- 每张表的 base value、head value、snapshot_id、schema_id、metadata_location。
- 是否满足 fast-forward publish。

算法：

```text
1. resolve base head 与 branch head。
2. 如果 base 是 branch 创建点或已记录 merge base，优先扫描 commit_ops。
3. 如果历史跨度过大，读取两个 ref index 做 content_key/value_id 对比。
4. 对同一 content_key 两边都变化的情况标为 CONFLICT。
```

P1 不解析完整 Iceberg schema diff 作为必需能力，但保留 `schema_id`、`spec_id`、`sort_order_id` 和 `metadata_fingerprint`，便于 P2 或 semantic merge service 使用。

### 6.7 Publish plan 与 fast-forward publish

P1 的 publish 是受控 fast-forward，不是 semantic merge。

```text
POST /api/v1/catalogs/{catalog}/branches/{branch}/publish-plan
{
  "targetRef": "main"
}
```

判断：

```text
if target_head == branch_base_or_expected_target:
  publishable = true
else:
  publishable = false
  return conflicts
```

执行 publish：

```text
POST /api/v1/catalogs/{catalog}/branches/{branch}/publish
{
  "targetRef": "main",
  "expectedTargetHead": "c123",
  "expectedSourceHead": "c456"
}
```

流程：

```text
1. 获取 targetRef 的 branch lock 或进入 merge queue。
2. 重新校验 source head 与 target head。
3. update catalog_refs set head_commit_id = source_head where ref_name = target and head_commit_id = expectedTargetHead。
4. 写 audit event: PUBLISH_FAST_FORWARD。
5. 写 outbox_materializations，用于把 main 物化到 Polaris。
6. 释放 lock。
```

如果 CAS 失败，说明 main 漂移，返回 409，并要求重新生成 publish-plan。

### 6.8 Polaris main 物化

P1 的权威状态在 Version Store，但为了兼容旧客户端和已有治理工具，需要把 main 的最新状态物化到 Polaris。

物化策略：

- 首选使用 Polaris / Iceberg REST 支持的多表 transaction commit，如果目标版本和部署支持。
- 如果只能逐表物化，必须在 outbox 中记录非原子物化边界，并把 Gateway 读写继续绑定 Version Store。
- 物化 worker 必须可重试、可对账、可暂停。
- 物化延迟目标：正常情况下 publish 后 **< 30s** 完成物化；失败时指数退避重试，最多 **5 次**，超出后写 `FAILED` 状态并触发告警。该目标是单实例 P1 的运行目标，不作为 Phase 6 前置阻塞条件；`FAILED` 状态和告警记录才是硬验收。
- 物化失败不回滚 Version Store 的 main HEAD；它会让 Polaris 兼容视图滞后，需通过运维告警处理。

对账流程：

```text
1. 读取 Version Store main head 对应 index。
2. 读取 Polaris 当前表 metadata pointer。
3. 比较每张表 metadata_location / snapshot_id。
4. 生成 drift report。
5. 对可自动修复项写新的 outbox event。
```

## 7. 工程实现设计

### 7.1 技术栈

P1 不继续以 Python/PowerShell 作为核心实现。技术栈已锁定如下：

| 层 | 选型（锁定） |
|---|---|
| 服务端语言 | Java 21 |
| 服务框架 | Quarkus 3.x（RESTEasy Reactive + CDI） |
| Iceberg 能力 | Apache Iceberg Java API 1.10.x（REST model、TableMetadata、FileIO） |
| 持久化 | PostgreSQL 16 + jOOQ 3.19.x |
| schema migration | Flyway 10.x |
| 集成测试 | JUnit 5 + Testcontainers 1.20.x |
| API 文档 | SmallRye OpenAPI（Quarkus 内置） |
| 可观测性 | Micrometer + OpenTelemetry（Quarkus 扩展） |
| 开发/运行环境 | 宿主机 Java 21 + Docker Compose：Gateway、PostgreSQL 16、Polaris 1.4.1、RustFS、Spark 3.5.8 |
| 构建工具 | Gradle Wrapper 8.14.5（`gradlew` / `gradlew.bat` 提交入库） |
| 可复现构建镜像 | `gradle:8-jdk21`，拉取并验证后作为 CI/容器构建选项 |

P1 单实例部署，Gateway HA 是 P2 范围。PowerShell 脚本保留为 Windows smoke workflow，不进入核心控制面。

### 7.2 代码模块建议

P1 模块位于 `Demo/p1_polaris_gateway/`，结构如下：

```text
Demo/p1_polaris_gateway/
  gateway-service/
    build.gradle.kts
    gradlew / gradlew.bat
    src/main/java/io/polaris/gateway/
      api/iceberg/         # Iceberg REST 子集资源类
      api/management/      # 管理 API 资源类
      ref/                 # RefResolver, RefContext
      store/               # RefRepository, CommitRepository, IndexRepository
      commit/              # CommitService, IcebergRequirementsValidator, IcebergMetadataWriter
      publish/             # PublishPlanner, PublishService, LockService
      polaris/             # PolarisClient, BootstrapService, MaterializationWorker, DriftChecker
      audit/               # AuditService
    src/main/resources/
      db/migration/        # Flyway SQL 文件
      openapi.yaml
    src/test/java/io/polaris/gateway/
  runtime/
    docker-compose.yml
    smoke/                 # smoke-phaseN.ps1 脚本
```

模块边界：

| 模块 | 主要包 |
|---|---|
| API layer | `api.iceberg`, `api.management` |
| Ref context | `ref.RefResolver`, `ref.RefContext` |
| Version store | `store.RefRepository`, `store.CommitRepository`, `store.IndexRepository` |
| Commit coordinator | `commit.CommitService`, `commit.IcebergMetadataWriter` |
| Publish coordinator | `publish.PublishPlanner`, `publish.PublishService`, `publish.LockService` |
| Polaris integration | `polaris.PolarisClient`, `polaris.MaterializationWorker` |
| Observability | `audit.AuditService`, `metrics.GatewayMetrics` |

### 7.3 API 设计

P1 管理 API 最小集合：

| API | 作用 |
|---|---|
| `POST /api/v1/catalogs/{catalog}/branches` | 创建 branch |
| `GET /api/v1/catalogs/{catalog}/branches` | 列出 branch/tag |
| `GET /api/v1/catalogs/{catalog}/branches/{branch}` | 查看 branch 元信息与 head |
| `DELETE /api/v1/catalogs/{catalog}/branches/{branch}` | 删除 branch 名称，数据保留交给 retention |
| `POST /api/v1/catalogs/{catalog}/branches/{branch}/diff` | 计算 diff |
| `POST /api/v1/catalogs/{catalog}/branches/{branch}/publish-plan` | 生成 fast-forward 发布计划 |
| `POST /api/v1/catalogs/{catalog}/branches/{branch}/publish` | 执行 fast-forward publish |
| `GET /api/v1/catalogs/{catalog}/commits/{commit}` | 查看 commit |
| `GET /api/v1/catalogs/{catalog}/refs/{ref}/contents` | 调试用，分页列出某 ref 的 content index |

P1 管理 API 必须支持：

- `Idempotency-Key`。
- request id / trace id。
- 明确的 409 conflict。
- 结构化错误体。
- actor 透传或服务端认证主体。

### 7.4 权限与绕写控制

P1 的权限模型先保持保守：

- 读表权限复用 Polaris 对 catalog/namespace/table 的权限判断。
- branch 创建、删除、publish 使用 Gateway 自身的 branch policy。
- `main` 默认受保护，普通写入只能进入 feature branch。
- Gateway 访问 Polaris 使用受控 service principal。
- Polaris endpoint 在生产网络中不暴露给普通引擎写入。

P1 文档和验收必须明确：如果部署允许客户端直接写 Polaris，Gateway 无法感知该提交，Version Store 的 branch HEAD 会失真。此场景不属于 P1 强一致承诺范围，只能通过 drift detection 发现。

### 7.5 GC 与 retention 边界

P1 不实现自动物理删除，但必须记录足够的 GC root：

- refs 的 head commit。
- tag 指向的 commit。
- branch/tag retention 期内的历史 commit。
- tombstone 对应的 dropped table metadata root。
- outbox 未完成物化事件引用的 content value。

P1 可以提供 GC dry-run：

```text
POST /api/v1/catalogs/{catalog}/gc/dry-run
```

输出不可达 commit、不可达 metadata location、潜在可清理对象数量。实际删除留到 P2 或独立 GC service。

## 8. 分阶段执行计划

### 8.0 Agent 执行约定

- 每个 Phase 由 Agent 独立执行，执行前必须确认前置条件已满足。
- Phase 之间唯一交接物是产物清单中列出的文件；不依赖上一 Phase 的上下文记忆。
- 每个 Phase 结束时，Agent 必须运行全部验收步骤并记录输出；输出不符合预期时停止，不继续下一 Phase。
- 所有路径以 `Demo/p1_polaris_gateway/` 为根（见 7.2 节）。
- 所有数据库变更只通过 Flyway migration 文件追加，不手动修改已执行的 migration。
- 若某步骤需要联网获取依赖（Maven Central、Docker Hub），见第 10 章环境与权限说明。

---

### Phase 1：项目骨架与接口冻结

**前置条件**：无（起点）。环境需满足第 10 章要求。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/build.gradle.kts` | Quarkus 3.x Gradle 项目，含 7.1 节全部依赖 |
| `gateway-service/gradlew` / `gateway-service/gradlew.bat` | Gradle Wrapper 8.14.5，可在宿主机 Java 21 或 `gradle:8-jdk21` 中执行 |
| `gateway-service/src/main/resources/openapi.yaml` | 7.3 节全部 API 端点，含请求/响应 schema |
| `gateway-service/src/main/resources/db/migration/V1__init.sql` | 第 5 节全部 10 张表的 DDL |
| `runtime/docker-compose.yml` | PostgreSQL 16、Polaris 1.4.1、RustFS、Gateway 占位服务 |
| `runtime/smoke/smoke-phase1.ps1` | 调用 `/q/health` 与 OpenAPI 端点验证骨架启动 |

**验收步骤**：

```powershell
# 1. 项目编译通过
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat compileJava

# 2. Flyway migration 含全部核心表（预期 >= 10）
(Select-String -Path src/main/resources/db/migration/V1__init.sql -Pattern '^CREATE TABLE').Count

# 3. OpenAPI 端点数量（预期 >= 15，覆盖管理 API + Iceberg REST 子集）
$job = Start-Job -ScriptBlock { Set-Location "$using:PWD"; .\gradlew.bat quarkusDev }
Start-Sleep -Seconds 15
$openapi = Invoke-RestMethod http://localhost:8080/q/openapi
$openapi.paths.PSObject.Properties.Count
Stop-Job $job

# 4. docker-compose 全部服务健康（预期 >= 4 个 running）
Set-Location ../runtime
docker compose up -d
Start-Sleep -Seconds 30
(docker compose ps --filter status=running --format json | ConvertFrom-Json).Count
```

---

### Phase 2：Version Store MVP

**前置条件**：Phase 1 完成（`V1__init.sql` 已存在，Quarkus 项目可编译）。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/src/main/java/io/polaris/gateway/store/RefRepository.java` | branch CRUD + CAS update + list（分页） |
| `gateway-service/src/main/java/io/polaris/gateway/store/CommitRepository.java` | insert commit + 按 commit_id 查询 |
| `gateway-service/src/main/java/io/polaris/gateway/store/IndexRepository.java` | insert/query index，Phase 2 实现 compaction 阈值判断（见 5.2 节） |
| `gateway-service/src/test/java/io/polaris/gateway/store/RefRepositoryIT.java` | Testcontainers PostgreSQL，覆盖以下验收场景 |

**验收步骤**：

```powershell
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat test --tests "*.store.*"
# 以下测试方法必须全部 PASS：
#   createBranch_success            — 创建成功，head_commit_id = source head
#   createBranch_duplicateName      — 同名 branch 第二次创建返回 409
#   casUpdate_succeeds              — expected head 匹配时 rows = 1
#   casUpdate_conflictsOnMismatch   — expected head 不匹配时 rows = 0，调用方收到明确错误
#   listBranches_excludesDeleted    — deleted=true 的 branch 不出现在列表中
```

---

### Phase 3：Iceberg REST load/list 与 bootstrap main

**前置条件**：Phase 2 完成（三个 Repository 测试全部 PASS）。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/src/main/java/io/polaris/gateway/ref/RefResolver.java` | 解析优先级：`X-Catalog-Ref` header > path segment > 默认 `main` |
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/ConfigResource.java` | `GET /iceberg/v1/config` |
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/NamespaceResource.java` | `GET /namespaces`，`GET /namespaces/{ns}` |
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/TableListResource.java` | `GET /namespaces/{ns}/tables`，走 index 不逐表 load |
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/TableLoadResource.java` | `GET /namespaces/{ns}/tables/{table}`，按 ref 解析 content value |
| `gateway-service/src/main/java/io/polaris/gateway/polaris/BootstrapService.java` | 从 Polaris 读取全量表生成初始 main commit + full index |
| `gateway-service/src/test/java/io/polaris/gateway/api/LoadTableIT.java` | Testcontainers 集成测试 |

**验收步骤**：

```powershell
# 1. 单元与集成测试
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat test --tests "*.api.*" --tests "*.ref.*"

# 2. 端到端（docker-compose 已启动）
# Bootstrap main
$bootstrap = Invoke-RestMethod -Method Post http://localhost:8080/api/v1/catalogs/test/bootstrap
$bootstrap.commitId
# 预期：HTTP 200，返回 {"commitId": "<commitId>"}

# 创建 branch dev_a（预期 HTTP 201）
$branchBody = @{ name = "dev_a"; sourceRef = "main" } | ConvertTo-Json
Invoke-RestMethod -Method Post http://localhost:8080/api/v1/catalogs/test/branches `
  -ContentType "application/json" `
  -Body $branchBody

# 验证 dev_a 与 main 初始 snapshot 一致
$main = Invoke-RestMethod http://localhost:8080/iceberg/v1/test/namespaces/sales/tables/orders `
  -Headers @{ "X-Catalog-Ref" = "main" }
$dev = Invoke-RestMethod http://localhost:8080/iceberg/v1/test/namespaces/sales/tables/orders `
  -Headers @{ "X-Catalog-Ref" = "dev_a" }
if ($main.metadata.'current-snapshot-id' -eq $dev.metadata.'current-snapshot-id') { "PASS" } else { throw "snapshot mismatch" }
```

---

### Phase 4：单表 Commit MVP

**前置条件**：Phase 3 完成（`loadTable` 可用，object storage 可写）。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/src/main/java/io/polaris/gateway/commit/IcebergRequirementsValidator.java` | 覆盖 Iceberg REST OpenAPI 的 8 类 table requirements |
| `gateway-service/src/main/java/io/polaris/gateway/commit/IcebergMetadataWriter.java` | 使用 FileIO 写新 metadata JSON 到 object storage |
| `gateway-service/src/main/java/io/polaris/gateway/commit/CommitService.java` | requirements 校验 → 写 metadata → DB 事务（content value + commit + index + CAS + audit + idempotency） |
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/TableCommitResource.java` | `POST /namespaces/{ns}/tables/{table}`，处理 `Idempotency-Key` header |
| `gateway-service/src/main/java/io/polaris/gateway/audit/AuditService.java` | 写 audit_events |
| `gateway-service/src/test/java/io/polaris/gateway/commit/CommitIT.java` | Testcontainers 集成测试，覆盖以下验收场景 |

**验收步骤**：

```powershell
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat test --tests "*.commit.*"
# 以下测试方法必须全部 PASS：
#   commitSucceeds_branchHasNewSnapshot   — branch load 返回新 snapshot
#   mainUnchangedAfterBranchCommit        — main load 仍返回旧 snapshot
#   idempotencyReplay_sameResponse        — 相同 Idempotency-Key 不生成新 commit
#   concurrentCommit_atLeastOneSucceeds   — 两个并发 commit 至少一个成功，另一个得到明确 409

# 验证 table requirements 类型覆盖（预期 = 8）
$validator = Get-Content src/main/java/io/polaris/gateway/commit/IcebergRequirementsValidator.java -Raw
@(
  "assert-create",
  "assert-table-uuid",
  "assert-ref-snapshot-id",
  "assert-last-assigned-field-id",
  "assert-current-schema-id",
  "assert-last-assigned-partition-id",
  "assert-default-spec-id",
  "assert-default-sort-order-id"
) | ForEach-Object {
  if ($validator -notmatch [regex]::Escape($_)) { throw "missing requirement: $_" }
}
"PASS"
```

---

### Phase 5：多表事务 commit 与 diff

**前置条件**：Phase 4 完成（单表 commit 全部验收通过）。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/src/main/java/io/polaris/gateway/api/iceberg/TransactionResource.java` | `POST /v1/{prefix}/transactions/commit`，多表一次 catalog commit |
| `gateway-service/src/main/java/io/polaris/gateway/diff/DiffService.java` | commit ops 路径（跨度 ≤ 50 commits）+ index 对比路径（跨度 > 50 commits） |
| `gateway-service/src/main/java/io/polaris/gateway/api/management/DiffResource.java` | `POST /api/v1/catalogs/{catalog}/branches/{branch}/diff` |
| `gateway-service/src/test/java/io/polaris/gateway/diff/DiffIT.java` | Testcontainers 集成测试 |

**验收步骤**：

```powershell
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat test --tests "*.diff.*" --tests "*.transaction.*"
# 以下测试方法必须全部 PASS：
#   multiTableTransaction_singleCatalogCommit  — 修改两张表，catalog_commits 只增加一条记录
#   diff_showsChangedAddedDeletedRenamed       — diff 正确列出四类变更
#   diff_usesCommitOpsNotFullScan              — 日志中无 object storage metadata GET（通过 mock FileIO 验证）
```

---

### Phase 6：Publish 与 Polaris 物化

**前置条件**：Phase 5 完成（diff 全部验收通过）。

**产物清单**：

| 文件路径 | 内容要求 |
|---|---|
| `gateway-service/src/main/java/io/polaris/gateway/publish/PublishPlanner.java` | 生成 fast-forward plan，标记 `publishable` 与 conflicts |
| `gateway-service/src/main/java/io/polaris/gateway/publish/PublishService.java` | 获取 lock → 重新校验 heads → ref HEAD CAS → 写 audit + outbox → 释放 lock |
| `gateway-service/src/main/java/io/polaris/gateway/publish/LockService.java` | 基于 `branch_locks` 表的乐观锁，含 fencing token |
| `gateway-service/src/main/java/io/polaris/gateway/polaris/MaterializationWorker.java` | Quarkus `@Scheduled`，读 outbox 物化到 Polaris，指数退避重试，最多 5 次 |
| `gateway-service/src/main/java/io/polaris/gateway/polaris/DriftChecker.java` | 对比 Version Store main index 与 Polaris 当前表指针，生成 drift report |
| `gateway-service/src/main/java/io/polaris/gateway/api/management/PublishResource.java` | `publish-plan` 与 `publish` 端点 |
| `gateway-service/src/test/java/io/polaris/gateway/publish/PublishIT.java` | Testcontainers 集成测试 |

**验收步骤**：

```powershell
Set-Location Demo/p1_polaris_gateway/gateway-service
.\gradlew.bat test --tests "*.publish.*"
# 以下测试方法必须全部 PASS：
#   publishSucceeds_mainRefUpdated          — main head 一次 CAS 到 source head，写 PUBLISH_FAST_FORWARD audit event
#   publishRejects_whenMainDrifted          — main 漂移时 publish-plan 返回 publishable=false + conflicts 列表
#   outboxRetry_completesAfterTransientFail — 物化 worker 第一次失败，第二次重试后 outbox 状态变 COMPLETED
#   driftReport_detectsMismatch            — Version Store main 与 Polaris main snapshot 不一致时 report 非空

# 端到端链路验证
$planBody = @{ targetRef = "main" } | ConvertTo-Json
$plan = Invoke-RestMethod -Method Post http://localhost:8080/api/v1/catalogs/test/branches/dev_a/publish-plan `
  -ContentType "application/json" `
  -Body $planBody
"publishable: $($plan.publishable)"
# 预期：publishable: true（前提：main 未漂移）

# 执行 publish（用实际 head id 替换占位符）
$publishBody = @{
  targetRef = "main"
  expectedTargetHead = "<main-head>"
  expectedSourceHead = "<dev_a-head>"
} | ConvertTo-Json
Invoke-RestMethod -Method Post http://localhost:8080/api/v1/catalogs/test/branches/dev_a/publish `
  -Headers @{ "Idempotency-Key" = "pub-e2e-001" } `
  -ContentType "application/json" `
  -Body $publishBody

# 30s 后验证物化完成，无 drift
Start-Sleep -Seconds 30
$drift = Invoke-RestMethod http://localhost:8080/api/v1/catalogs/test/drift-report
"drift items: $($drift.items.Count)"
# 预期：drift items: 0
```

## 9. 测试与验收

### 9.1 单元测试

必须覆盖：

- branch create/list/delete。
- ref resolve 默认值、header、path fallback。
- commit CAS conflict。
- idempotency replay。
- diff index。
- namespace/table lookup。
- publish-plan fast-forward 与 conflict。
- outbox retry 状态转换。

### 9.2 集成测试

使用 Testcontainers 启动：

- PostgreSQL 16。
- Polaris 1.4.1。
- RustFS。
- Spark 3.5+ with Iceberg 1.10.x。

测试链路：

```text
bootstrap main
create dev_a/dev_b
load table on each ref
commit table on dev_a
multi-table commit on dev_b
diff dev_a/main
publish-plan dev_a
publish dev_a
verify dev_b publish conflict
```

### 9.3 端到端验收标准

P1 完成的最低标准：

- `dev_a` 与 `dev_b` 可以从 `main` O(1) 创建。
- 同一张表在不同 ref 上可以读到不同 snapshot。
- branch commit 后 main 不变化。
- 两张表一次 transaction commit 生成同一个 catalog commit。
- main 漂移时 publish-plan 拒绝 fast-forward。
- publish 成功后 Gateway main 立即可见；Polaris main 通过 outbox 最终物化。
- 直接绕过 Gateway 写 Polaris 的场景被明确标记为不受 P1 保证，并可被 drift report 检测。

### 9.4 性能验收

P1 需要做小规模基准，目标不是极限吞吐，而是证明架构不会随 catalog 表数量线性退化。

| 场景 | 验收目标 |
|---|---|
| branch create | O(1)，不扫描全表。 |
| table load | 通过 `(index_id, content_key)` 查找，单表 lookup 不回放历史。 |
| namespace list | 通过 `(index_id, namespace)` 分页列出，不逐表 load metadata。 |
| diff | 优先走 commit ops，必要时走 index 对比，不采集每张表 metadata table。 |
| publish | 一次 CAS 推进 target ref，不逐表 fast-forward 作为权威边界。 |

## 10. 环境需求与当前验证状态

截至 2026-05-21，宿主机环境已经满足 P1-MVP-A 的开发前置条件。当前验证结果如下：

| 类别 | 当前状态 | P1 影响 |
|---|---|---|
| 基础工具 | Git、Docker Desktop、Java 21、Python、Chocolatey 可用 | 可以直接开发；宿主机 Java 21 与技术栈一致。 |
| 宿主机构建工具 | `gradle`、`mvn`、`psql` 未安装到 PATH | 不阻塞；P1 使用 `gradlew.bat` + 宿主机 Java 21，psql 从 `postgres:16` 容器使用。 |
| 已验证 Docker 镜像 | `gradle:8-jdk21`、`postgres:16`、`apache/polaris:1.4.1`、`apache/spark:3.5.8-java17-python3`、`rustfs/rustfs:1.0.0-alpha.81`、`alpine/curl:8.17.0`、`amazon/aws-cli:2.34.9` 已存在 | P1 runtime、容器化构建与集成测试可执行。`gradle:8-jdk21` 已验证为 Gradle 8.14.5 + Java 21.0.11；Spark 镜像使用 Java 17 只影响外部测试客户端，不影响 Gateway Java 21。 |
| P0 runtime | Polaris/RustFS 当前 healthy，端口 `8181/8182/9000/9001` 被占用 | P1 可复用它做 bootstrap 与对照测试；若释放资源可停掉 P0 compose。 |
| P1 端口 | `8080/8081/5432/55432` 当前空闲 | Gateway 与 PostgreSQL P1 runtime 可直接使用这些端口。 |
| 参考源码 | `src_ref/polaris`、`src_ref/iceberg`、`src_ref/nessie` 已并列放置并被根 `.gitignore` 排除 | Agent 可在宿主机查阅 Polaris、Iceberg REST 与 Nessie version store 代码，不污染本仓库。 |
| 依赖仓库 | 已验证可从 Maven Central 拉取 Iceberg 1.10.1 与 Quarkus BOM | Maven 只作为依赖仓库语境；P1 构建工具统一使用 Gradle Wrapper。 |

P1 开发默认约定：

- 快速开发默认使用宿主机 Java 21 + `gateway-service/gradlew.bat`；不要求宿主机安装 Gradle、Maven、PostgreSQL。
- Gateway 服务端使用 Java 21 + Quarkus + PostgreSQL + Flyway + Iceberg Java API（见 7.1 节）；容器构建统一使用已验证的 `gradle:8-jdk21`。
- 继续保留 P0 Polaris/RustFS runtime，直到 Phase 3 bootstrap/list/load 完成；之后可按需要停止。
- 如果后续权限切回自动审查模式，需要批准 `docker ps`、`docker compose`、`docker run`、`docker pull` 等命令，否则 Agent 只能做代码编辑和静态检查，无法自动跑容器集成测试。
- 生产或准生产验证仍要求 Gateway 是唯一写入口，Polaris 直写入口必须通过网络或权限封闭；否则 P1 只能保证 Gateway 内部一致性。

用户可能需要手动处理的事项只有两类：第一，保持 Docker Desktop 运行；第二，在自动审查权限下批准必要 Docker 命令。宿主机 Maven、Gradle、PostgreSQL 暂不需要安装；宿主机 Java 21 已满足开发入口。

## 11. 工作量估计与一周 MVP 边界

按单 Agent 估算，完整 P1 从零到“Spark 可稳定写入、multi-table transaction、publish/outbox 可恢复”的工作量约为 **15-24 个工程日**。主要不确定性集中在 Iceberg REST commit 兼容、metadata 写入、Spark 行为验证和 Polaris main 物化。

| 阶段 | 内容 | 估算 |
|---|---|---:|
| Phase 1 | Quarkus/Gradle 骨架、OpenAPI、Docker Compose、Flyway schema | 1 天 |
| Phase 2 | PostgreSQL Version Store、branch API、CAS、idempotency 基础 | 1.5-2 天 |
| Phase 3 | bootstrap main、ref-indexed namespace/table list、load table | 2-3 天 |
| Phase 4 | Spark-compatible 单表 commit | 3-5 天 |
| Phase 5 | multi-table transaction 与 diff | 2-4 天 |
| Phase 6 | publish-plan、fast-forward publish、outbox、Polaris materialization | 3-5 天 |
| 硬化 | 集成测试、错误语义、文档、可观测性 | 3-5 天 |

一周内建议交付 **P1-MVP-A**，目标到 Phase 3，并完成 Phase 4 的窄口径 spike：

- Gateway 项目骨架可启动。
- PostgreSQL migration 完成。
- Branch 管理 API 可用：create/list/get/delete。
- Version Store 具备 refs、commits、content values、index、idempotency 基础表。
- bootstrap 能从当前 Polaris main 导入表状态。
- Iceberg REST `config`、namespace list、table list、load table 基本可用。
- 单表 commit 做到受控测试请求或最小 Spark 写入 spike，但不承诺完整 Iceberg REST commit 兼容。

一周内不建议承诺：

- Spark 端完整写入闭环稳定。
- multi-table transaction。
- publish/outbox/Polaris main 物化闭环。
- Trino/Flink/PyIceberg 兼容。
- GC/retention 自动清理。

## 12. 与 P0/P2 的关系

P0、P1、P2 的边界如下：

| 阶段 | 权威状态 | 读写入口 | branch 成本 | publish 边界 | 适用性 |
|---|---|---|---|---|---|
| P0 | JSON manifest | 脚本 + Spark SQL + Polaris | O(branch × table) | 逐表 fast-forward SQL | workflow demo |
| P1 | 外置 PostgreSQL Version Store | Branch-aware Iceberg REST Gateway | O(1) ref pointer | ref HEAD CAS + outbox materialization | 可迁移架构验证 |
| P2 | Polaris 内置 Version Store | Polaris 原生 ref-aware API | O(1) ref pointer | Polaris 内部 catalog commit | 最终产品形态 |

P1 的价值是证明“在主流 Catalog 周围收敛写入口，并引入外置权威 version store”能否支撑真实规模。P1 成功后，P2 不应迁移 Python/PowerShell 或 P0 manifest，而应迁移 P1 的语义、schema、CAS、index、audit、outbox 与 API 行为。

## 13. 参考资料

- Apache Iceberg REST Catalog OpenAPI：<https://github.com/apache/iceberg/blob/main/open-api/rest-catalog-open-api.yaml>
- Apache Iceberg REST Catalog Spec：<https://iceberg.apache.org/rest-catalog-spec/>
- Apache Iceberg Catalog Properties：<https://iceberg.apache.org/docs/nightly/catalog-properties/>
- Apache Polaris Releases：<https://polaris.apache.org/downloads/>
- Apache Polaris Documentation：<https://polaris.apache.org/docs/>
- Apache Polaris 1.4.1 Documentation：<https://polaris.apache.org/releases/1.4.1/>
- Apache Gravitino Iceberg REST Service：<https://gravitino.apache.org/docs/next/iceberg-rest-service/>
- Delta Catalog-Managed Tables：<https://docs.delta.io/delta-catalog-managed-tables/>
- Unity Catalog Managed Tables：<https://unitycatalog.io/blogs/introducing-unity-catalog-managed-tables/>
- Project Nessie Iceberg REST Guide：<https://projectnessie.org/guides/iceberg-rest/>
- Project Nessie CLI：<https://projectnessie.org/nessie-latest/cli/>
- Project Nessie Configuration：<https://projectnessie.org/nessie-latest/configuration/>
